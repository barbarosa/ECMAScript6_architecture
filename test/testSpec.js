/**
 * Unit test setup spec
 * Uses ES6: module imports; arrow functions;
 */

'use strict';

import { expect } from 'chai';

describe('Correct unit test setup', () => {
  it('Should work', () => {
    expect('abc').to.equal('abc');
  });
});
